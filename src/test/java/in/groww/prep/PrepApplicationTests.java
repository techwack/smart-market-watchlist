package in.groww.prep;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — keep this passing at all times during the hackathon.
 * A green build you can point to beats a fancier feature judges can't verify.
 */
@SpringBootTest
class PrepApplicationTests {

    @Test
    void contextLoads() {
        // If this fails, nothing else you built matters yet — fix this first.
    }
}
